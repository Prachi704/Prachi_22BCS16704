class Solution {
public:
    int l=0;
    void moveZeroes(vector<int>& nums) {
        for(int i=0;i<nums.size();i++)
        {
         if(nums[i]!=0)
         {
            swap(nums[l],nums[i]);
         l++;
      
          }
       }  
    }
};
